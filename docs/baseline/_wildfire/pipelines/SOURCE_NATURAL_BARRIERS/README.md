# Pipeline - SOURCE_NATURAL_BARRIERS

Access option verified: `ACCESS_NATURAL_BARRIERS_PRIMARY`.

The source uses the same `국토교통부 국토지리정보원_기본공간정보` file-download option. For barrier use, the verified public metadata supports line/polygon water-system and topographic feature classes. Open-space barrier coverage is only partial until the exact NGII target feature classes are downloaded and inspected.

Output captured for this transaction:

```text
data/raw/SOURCE_NATURAL_BARRIERS/snapshots/sample_20260430/
  access_option_verification.json
  dcat.xml
  source_page.md
```

Raw-phase constraints:

- no barrier candidate derivation
- no segment join
- no CRS conversion

